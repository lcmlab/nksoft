<% @page_title = "顧客情報の編集" %>

<h1><%= @page_title %></h1>

<p><%= link_to "顧客の詳細に戻る", [:admin, @client] %></p>

<%= form_for [:admin, @client], html: { multipart: true } do |form| %>
<%= render "form", form: form %>
<p><%= form.submit %></p>
<% end %>
