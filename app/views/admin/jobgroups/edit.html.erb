<% @page_title = "業務区分情報の編集" %>

<h1><%= @page_title %></h1>

<p><%= link_to "業務区分の詳細に戻る", [:admin, @jobgroup] %></p>

<%= form_for [:admin, @jobgroup], html: { multipart: true } do |form| %>
<%= render "form", form: form %>
<p><%= form.submit %></p>
<% end %>
