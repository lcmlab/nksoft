<% @page_title = "状況情報の編集" %>

<h1><%= @page_title %></h1>

<p><%= link_to "状況の詳細に戻る", [:admin, @state] %></p>

<%= form_for [:admin, @state], html: { multipart: true } do |form| %>
<%= render "form", form: form %>
<p><%= form.submit %></p>
<% end %>
